
logging:
  path: d:/
  root:
    level: debug
  config: classpath:logback-spring-dev.xml

server:
  port: 8888

filePath: D:\files

test:
      name : jerry
      age: 19

spring:
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java #重启目录
      exclude: WEB-INF/**
  freemarker:
    cache: false
  thymeleaf:
    cache: false

